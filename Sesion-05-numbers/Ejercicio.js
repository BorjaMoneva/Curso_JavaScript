const altura_cm = 181;
const altura_m = 1.81;
const peso_kg = 90.3;

const altura_redondeada = Math.ceil(altura_m);
const peso_redondeado = Math.floor(peso_kg);

const iguales = Number.MAX_VALUE + 1 === Number.MAX_VALUE;